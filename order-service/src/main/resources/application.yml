server:
  port: 8081
spring:
  application:
    name: order-service
  # Database 설정
  datasource:
    url: jdbc:postgresql://localhost:5432/ordersense
    username: ordersense
    password: ordersense123
    driver-class-name: org.postgresql.Driver
  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: update  # 개발 중에는 update, 운영에서는 validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Redis 설정
  data:
    redis:
      host: localhost
      port: 6379

  # Kafka 설정
  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all  # 모든 replica가 확인할 때까지 대기
      retries: 3
    consumer:
      group-id: order-service-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "*"

# Actuator 설정 (Prometheus 메트릭)
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    export:
      prometheus:
        enabled: true

# Logging 설정 (ELK)
logging:
  level:
    com.ordersense: DEBUG
    org.springframework.kafka: INFO
    org.hibernate.SQL: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
  file:
    name: logs/order-service.log

# Sentry 설정 (에러 트래킹)
sentry:
  dsn: https://bb46834c848680e10cba41a064594039@o4510420179812352.ingest.us.sentry.io/4510420181319680
  traces-sample-rate: 1.0
  environment: ${ENVIRONMENT:local}